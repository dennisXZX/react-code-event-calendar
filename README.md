# React Code Event Calendar
Built with React, React-router and React-icons

## Steps
- run `npm install` to install the dependencies
- run `npm start` to run the app server
- default port: `localhost:8080`